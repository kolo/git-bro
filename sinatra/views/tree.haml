= partial :menu, :locals => {:branch => @branch, :active => :browse}
= partial :commit_details, :locals => {:commit => @top_commit, :link_sha => true}
%div#path
  - render_path(@path, @branch)
= partial :dir, :locals => {:path_prefix => @url_prefix, :objs => @objs}
